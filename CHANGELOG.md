# لیست تغییرات (Changelog)

تمامی تغییرات و پیشرفت‌های پروژه در این فایل ثبت می‌شود.

## [0.1.0] - 1404-11-08
### زیرساخت اولیه
- راه‌اندازی اولیه پروژه و ساختار پوشه‌ها.
- تنظیم محیط مجازی و نصب پکیج‌ها.
- طراحی دیتابیس (Models) و تنظیمات اتصال (Database).
- پیاده‌سازی ابزارهای امنیتی (Security Utils).

## [0.2.0] - 1404-11-08
### اضافه شده (Authentication System)
- ساخت فایل اصلی `main.py` و راه‌اندازی سرور FastAPI.
- پیاده‌سازی سیستم ثبت‌نام کاربر (`/register`) با چک کردن نام کاربری تکراری.
- پیاده‌سازی سیستم لاگین (`/token`) و تولید توکن JWT.
- افزودن قابلیت Authorize در Swagger UI (دکمه قفل بالا سمت راست).
- تعیین نقش (Role) برای کاربران: اولین کاربر به صورت خودکار `admin` می‌شود.
- محافظت از روت‌ها با وابستگی `get_current_user`.

## [0.3.0] - 1404-11-08
### اضافه شده (Core Logic & Security)
- پیاده‌سازی کامل APIهای مدیریت تسک (Jobs): ساخت، مشاهده لیست، و تغییر وضعیت توسط ادمین.
- ارتقای امنیت پسوردها به الگوریتم `Argon2` (رفع باگ محدودیت کاراکتر bcrypt).
- ساخت `worker.py`: شبیه‌ساز پردازش تسک‌ها در پس‌زمینه (Pending -> Running -> Completed).
- آماده‌سازی پوشه `templates` برای رابط کاربری وب.

## [0.4.0] - 1404-11-08
### Added
- 🎨 رابط کاربری (Frontend) کامل برای داشبورد.
- 🔐 اضافه شدن سطح دسترسی `is_admin` به دیتابیس کاربران.
- ✅ دکمه "تایید درخواست" در داشبورد (مخصوص مدیران).
- 🔄 نمایش وضعیت لحظه‌ای تسک‌ها بدون نیاز به رفرش.
- 🛡️ گارد امنیتی برای اندپوینت‌های حساس (فقط ادمین می‌تواند تایید کند).

### Fixed
- 🐛 رفع مشکل `NameError: HTMLResponse`.
- 🐛 اصلاح خطای اعتبارسنجی `role` و جایگزینی با `is_admin` در سیستم توکن.
- 🔧 رفع باگ عدم نمایش قالب‌ها (`jinja2`).


## [0.5.0] - 2026-01-29 
### ✨ ویژگی‌های جدید (Added)
- **سیستم تایید دو مرحله‌ای:** افزودن منطق Admin Approval؛ درخواست‌ها ابتدا "در انتظار" هستند و پس از تایید ادمین اجرا می‌شوند.
- **نوار پیشرفت (Progress Bar):** نمایش گرافیکی وضعیت اجرای تسک در داشبورد بصورت انیمیشن.
- **بروزرسانی خودکار (Auto-Refresh):** لیست درخواست‌ها هر ۲ ثانیه بصورت خودکار وضعیت جدید را دریافت می‌کند.
- **اسکریپت ادمین ساز:** افزودن فایل `make_admin.py` برای تبدیل آسان کاربر عادی به مدیر.

### 🎨 تغییرات ظاهری (UI/UX)
- **داشبورد ثابت:** تبدیل فرم ثبت درخواست از حالت Pop-up (مخفی) به حالت Static (همیشه در دسترس).
- **بهبود جدول‌ها:** افزایش خوانایی جدول با استایل Zebra (یکی در میان)، رنگ‌بندی وضعیت‌ها و فونت مخصوص برای کدها.
- **حذف باگ‌های بصری:** رفع مشکل بهم‌ریختگی متن‌ها و تو رفتگی سطرها.

### 🐛 باگ‌فیکس‌ها (Fixed)
- **ساختار پروژه:** اصلاح مسیر پوشه‌های `static` و `templates` و انتقال آن‌ها به ریشه پروژه برای حل مشکل `Directory not found`.
- **موتور پردازش (Worker):** اصلاح فایل `worker.py` برای اتصال صحیح به دیتابیس و پردازش فقط تسک‌های تایید شده.
- **امنیت:** رفع مشکل ایمپورت `OAuth2PasswordRequestForm` در `main.py`.

## [0.8.0] - 2026-01-30 (UI/UX Overhaul)
### ✨ ویژگی‌های جدید (Added)
- **آیکون‌های گرافیکی (Vector Icons):** جایگزینی تمام ایموجی‌ها با `Bootstrap Icons` برای ظاهر حرفه‌ای‌تر.
- **چشم رمز عبور (Password Toggle):** افزودن دکمه مشاهده رمز عبور به صورت شناور داخل فیلد ورودی.
- **سنجش قدرت رمز عبور:** نمایش گرافیکی قدرت رمز عبور با چک‌لیست تعاملی (تیک سبز و ضربدر).
- **پاکسازی خودکار:** ریست شدن فرم ثبت‌نام پس از ساخت موفقیت‌آمیز حساب کاربری.

### 🎨 بهبود رابط کاربری (UI Improvements)
- **ریسپانسیو (Responsive):** بهینه‌سازی کامل فرم‌ها و جداول برای نمایش صحیح در موبایل و تبلت.
- **تراز بندی (Alignment):** اصلاح دقیق فاصله متن‌ها و آیکون‌ها در هدر، دکمه‌ها و بج‌ها با استفاده از `Flexbox` و `Gap`.
- **اصلاح دکمه‌های ورود/ثبت‌نام:** وسط‌چین کردن و هم‌اندازه کردن تب‌ها بدون کشیدگی.

### 🐛 باگ‌فیکس‌ها (Fixed)
- رفع مشکل چسبیدن نام کاربری به نقش کاربر در داشبورد.
- رفع مشکل ناخوانا بودن متن قوانین رمز عبور (تغییر رنگ از خاکستری تیره به روشن).
- اصلاح مشکل سفید شدن دکمه‌های فعال در تم تیره.