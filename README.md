# سامانه مدیریت سرویس‌های GPU (پروژه پایان‌ترم)

این پروژه یک وب‌سرویس (Web Service) برای مدیریت درخواست‌ها و تخصیص منابع پردازشی است که به عنوان پروژه درس **برنامه‌نویسی پیشرفته** با زبان پایتون توسعه داده شده است.

## 📋 ویژگی‌های کلیدی
- **احراز هویت امن:** ثبت‌نام و ورود کاربران با استفاده از توکن `JWT`.
- **مدیریت نقش‌ها:** تفکیک سطح دسترسی بین کاربر عادی (User) و مدیر (Admin).
- **مدیریت تسک‌ها (Jobs):** ثبت درخواست GPU با جزئیات کامل (مدت زمان، نوع گرافیک و...).
- **شبیه‌ساز (Simulator):** اجرای مجازی تسک‌ها بدون نیاز به سخت‌افزار واقعی.
- **رابط کاربری (Web UI):** پنل کاربری ساده و کاربردی (در حال توسعه).

## 🛠 تکنولوژی‌های استفاده شده
- **هسته اصلی:** Python 3.10+
- **فریم‌ورک وب:** FastAPI
- **دیتابیس:** SQLite (با ORM قدرتمند SQLAlchemy)
- **امنیت:** Passlib (هش پسورد) و PyJWT (توکن)

## 🚀 راهنمای نصب و اجرا (نسخه توسعه)

فعلاً برای اجرای پروژه در محیط لوکال (بدون داکر)، مراحل زیر را طی کنید:

۱. **نصب پیش‌نیازها:**
```bash
pip install -r requirements.txt

