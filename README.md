# سامانه مدیریت سرویس‌های GPU (پروژه پایان‌ترم)

این پروژه یک پلتفرم وب (Web Service) برای مدیریت درخواست‌ها و تخصیص منابع پردازشی است. این سامانه به کاربران اجازه می‌دهد درخواست‌های GPU خود را ثبت کنند و ادمین سیستم آن‌ها را مدیریت کند.
پروژه با معماری تمیز (Clean Architecture) پیاده‌سازی شده و از قابلیت‌های مدرن پایتون استفاده می‌کند.

## 📋 ویژگی‌های کلیدی
- **معماری ماژولار:** تفکیک منطق برنامه در پکیج `app` برای نظم و توسعه‌پذیری.
- **احراز هویت امن:** استفاده از **JWT** برای لاگین و الگوریتم پیشرفته **Argon2** برای هش کردن پسوردها.
- **سیستم صف و شبیه‌ساز (Simulator):** پردازش پس‌زمینه تسک‌ها توسط `worker.py` (شبیه‌سازی تغییر وضعیت از Pending به Running و Completed).
- **مدیریت نقش‌ها (RBAC):** تفکیک دسترسی بین کاربر عادی و ادمین.
- **رابط کاربری (UI):** استفاده از قالب‌های HTML/Bootstrap (در حال تکمیل).

## 🛠 تکنولوژی‌های استفاده شده
- **Backend:** Python 3.10+, FastAPI
- **Database:** SQLite, SQLAlchemy ORM
- **Security:** Passlib (Argon2), PyJWT, OAuth2
- **Frontend:** Jinja2 Templates, Bootstrap 5

## 🚀 راهنمای نصب و اجرا

برای اجرای کامل پروژه، مراحل زیر را دنبال کنید:

### ۱. نصب پیش‌نیازها
ابتدا در پوشه اصلی پروژه، پکیج‌های مورد نیاز را نصب کنید:
```bash
pip install -r requirements.txt


۲. اجرای سرور اصلی (Web Server)
در یک ترمینال، دستور زیر را بزنید تا وب‌سایت بالا بیاید:

uvicorn main:app --reload

آدرس سایت: http://127.0.0.1:8000

آدرس مستندات (Swagger): http://127.0.0.1:8000/docs

۳. اجرای شبیه‌ساز (Worker)
برای اینکه تسک‌ها پردازش شوند، باید شبیه‌ساز را در یک ترمینال جداگانه اجرا کنید:

python worker.py
(این اسکریپت به صورت مداوم دیتابیس را چک می‌کند و تسک‌های تایید شده را اجرا می‌کند)

نحوه تست سناریوی اصلی (Workflow)
۱. وارد صفحه Swagger (/docs) شوید. ۲. یک کاربر جدید ثبت‌نام کنید (/register). اولین کاربر به صورت خودکار ادمین می‌شود. ۳. با دکمه Authorize و وارد کردن نام کاربری/رمز عبور لاگین کنید. ۴. یک تسک جدید بسازید (POST /jobs). وضعیت اولیه PENDING خواهد بود. ۵. (به عنوان ادمین) وضعیت تسک را به APPROVED تغییر دهید (PUT /jobs/{id}). ۶. به ترمینالِ worker.py نگاه کنید؛ باید ببینید که تسک شروع به اجرا می‌کند و پس از مدت مشخصی تمام می‌شود.

توسعه داده شده توسط: ماهان صادقی و مونا تولمی


