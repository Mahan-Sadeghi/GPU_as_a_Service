# سامانه مدیریت پردازش ابری (GPU as a Service) ☁️⚙️

![Python](https://img.shields.io/badge/Python-3.9-blue?style=flat&logo=python)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109-009688?style=flat&logo=fastapi)
![Docker](https://img.shields.io/badge/Docker-Enabled-2496ED?style=flat&logo=docker)
![Tests](https://img.shields.io/badge/Tests-Passing-success?style=flat)

این پروژه یک سرویس واسط برای مدیریت و سهمیه‌بندی منابع پردازشی (GPU) است که به عنوان پروژه درس مهندسی نرم‌افزار / برنامه‌نویسی پیشرفته طراحی و پیاده‌سازی شده است. سیستم قابلیت ثبت درخواست، صف‌بندی، تایید توسط ادمین و اجرای شبیه‌سازی‌شده روی کانتینرها را دارد.

---

## ✨ ویژگی‌های کلیدی
- 🔐 احراز هویت امن: سیستم لاگین و ثبت‌نام با استفاده از JWT و هشینگ رمز عبور.
- 🛡 سطح دسترسی: تفکیک کامل پنل و دسترسی‌های Admin و User.
- 📊 داشبورد مدیریتی: مشاهده وضعیت درخواست‌ها، نمودار مصرف سهمیه و مدیریت تسک‌ها.
- 🎨 رابط کاربری مدرن: طراحی نئونی (Dark Mode) با استفاده از Bootstrap و پاپ‌آپ‌های انیمیشنی.
- ⏱️ سیستم سهمیه‌بندی: کسر اعتبار بر اساس زمان اجرا و جلوگیری از درخواست بیش از حد.
- 🐳 داکرایز شده: آماده اجرا روی هر سیستم با استفاده از docker-compose.
- 🧪 تست‌های خودکار: شامل تست‌های واحد (Unit Tests) برای اطمینان از صحت عملکرد.

---

## 🏗 معماری سیستم
این پروژه بر اساس معماری Client-Server و RESTful API بنا شده است:
- Backend: فریم‌ورک FastAPI (عملکرد بالا و اعتبارسنجی خودکار).
- Database: دیتابیس SQLite با استفاده از SQLAlchemy ORM.
- Worker: شبیه‌سازی اجرای پردازش‌های سنگین به صورت Background Task.
- Frontend: رندرینگ سمت سرور با Jinja2 و تعاملات AJAX.

*(جزئیات بیشتر در فایل ARCHITECTURE.md موجود است)*

---

## 🚀 راهنمای نصب و اجرا

### روش پیشنهادی: استفاده از Docker 🐳
اگر داکر را نصب دارید، تنها با یک دستور کل پروژه (دیتابیس + سرور) اجرا می‌شود:

```bash
docker-compose up --build
```
سپس مرورگر را باز کرده و به آدرس زیر بروید: 👉 http://127.0.0.1:8000


روش دوم  (بدون Docker) 🐍
اگر مایل به اجرای دستی هستید:

1. نصب پیش‌نیازها:

```bash
pip install -r requirements.txt
```
2. اجرای سرور:

```bash
uvicorn main:app --reload
```
🧪 اجرای تست‌ها
برای بررسی صحت عملکرد سیستم و اجرای تست‌های خودکار، دستور زیر را در ترمینال وارد کنید:

```bash
pytest
```
📂 ساختار فایل‌ها

```bash
gpu_project/
├── main.py              # نقطه ورودی برنامه
├── worker.py            # سرویس شبیه‌ساز پردازش
├── requirements.txt     # لیست کتابخانه‌ها
├── Dockerfile           # تنظیمات ایمیج داکر
├── docker-compose.yml   # تنظیمات اجرای کانتینری
├── ARCHITECTURE.md      # مستندات معماری
├── CHANGELOG.md         # تاریخچه تغییرات پروژه
├── templates/           # فایل‌های HTML
├── static/              # فایل‌های استاتیک (CSS/JS)
├── tests/               # تست‌های خودکار
└── gpu_service.db       # دیتابیس پروژه
```

👤 اطلاعات دانشجو

نام دانشجویان: ماهان صادقی - مونا تولمی

شماره دانشجویی: ماهان 1400111024035

مونا 1400111024106

درس: برنامه‌نویسی پیشرفته(Python)



