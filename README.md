# 🚀 سامانه مدیریت پردازش ابری (GPU Service Manager)

این پروژه یک سیستم کامل برای مدیریت درخواست‌های پردازشی سنگین (مانند آموزش مدل‌های هوش مصنوعی) است. کاربران می‌توانند درخواست اجاره کارت گرافیک بدهند و مدیر سیستم پس از بررسی، آن‌ها را تایید کند.

## ✨ ویژگی‌های کلیدی
- **Backend:** نوشته شده با **FastAPI** (سریع و مدرن).
- **Frontend:** داشبورد تعاملی با **Bootstrap** و **Vanilla JS**.
- **Database:** استفاده از **SQLite** با **SQLAlchemy**.
- **Security:** احراز هویت با **JWT** و هشینگ پسورد با **Argon2/Bcrypt**.
- **Role-Based Access:** سطح دسترسی متفاوت برای **کاربر عادی** و **مدیر سیستم**.
- **Real-time Updates:** مشاهده وضعیت لحظه‌ای تسک‌ها (Pending -> Approved -> Running -> Completed).
- **Worker Simulation:** شبیه‌سازی پردازش‌های پس‌زمینه.

## 🛠 نصب و اجرا

1. **فعال‌سازی محیط مجازی:**
```bash
   python -m venv venv
   # Windows:
   venv\Scripts\activate
   # Linux/Mac:
   source venv/bin/activate

2. نصب وابستگی‌ها:

pip install fastapi uvicorn sqlalchemy jinja2 passlib python-multipart

3. اجرای سرور اصلی:

uvicorn main:app --reload

4. اجرای پردازشگر (Worker): (در یک ترمینال جداگانه):

python worker.py

نحوه ورود (حساب ادمین)
برای دسترسی به پنل مدیریت، هنگام ثبت‌نام از نام کاربری admin استفاده کنید. سیستم به صورت خودکار دسترسی مدیریت را فعال می‌کند.

توسعه داده شده توسط: ماهان صادقی و مونا تولمی



